<h1>Listing projects</h1>

<table>
  <tr>
    <th>Name</th>
    <th>State</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @projects.each do |project| %>
  <tr>
    <td><%= project.name %></td>
    <td><%= project.state %></td>
    <td><%= link_to 'Show', customer_project_path(@customer, project) %></td>
    <td><%= link_to 'Edit', edit_customer_project_path(@customer, project) %></td>
    <td><%= link_to 'Destroy', customer_project_path(@customer, project), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Project', new_customer_project_path %>

